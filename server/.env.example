PORT=5000
NODE_ENV=production
JWT_SECRET=super_secret_jwt_key_fintech_2026

# PostgreSQL Connection String (For Render / Railway / Supabase / Neon deployment)
# Example: postgres://username:password@ep-host.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=
USE_POSTGRES=true

# Client URL allowed for CORS
CLIENT_URL=http://localhost:5173
